app:
  name: "course-service"

server:
  host: "localhost"
  port: 50055
  timeout:
    shutdown: "10s"

service:
  school:
    name: "school-service"
    host: "localhost"
    port: 50053

database:
  host: "localhost"
  port: 5432
  user: ""
  pass: ""
  name: "klasshub_course_db"
  ssl_mode: "disable"
  max_conns: 5
  min_conns: 1
  max_conn_lifetime: "1h"
  max_conn_idle_time: "30m"

tracer:
  host: "localhost"
  port: 4317
